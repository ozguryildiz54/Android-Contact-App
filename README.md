Android Contact Application

Keep all your contacts at your fingertips with this Android application! This project demonstrates contact management on mobile devices.

Note: Large build files have been excluded to keep the repository lightweight. To run the project in Android Studio, simply clone the repository and sync the Gradle files; the IDE will automatically generate the necessary build components.
